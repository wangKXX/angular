import { CssHighlightDirective } from './css-highlight.directive';

describe('CssHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CssHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
